{
  "name": "Remove Amazon Browsing History",
  "description": "Amazon removed the button to clear all browsing history at once. This extension adds back the function to bulk remove the history.",
  "version": "0.1.0",
  "manifest_version": 3,
  "permissions": ["activeTab", "scripting"],
  "action": {
    "default_popup": "src/popup.html"
  }
}
